Este algoritmo iserta un nodo despues del otro dad como referencia, si la referencia es menor al nuevo nodo,

 * creamos p com apuntador  del primer nodo
 * creamos f com apuntador  del segundo nodo
 * k representa el contenido del nuevo nodo
 * r y z son variables apuntador, las cuales se van a desplazar en la lista.
 * num y liga son campos de los nodo de la lista


1. si (p = NULL) 
   entonces
      return f;
2. fin de la condicion del paso 1
3. si (f = NULL)
   entonces
      return p;
4. fin de la condicion del paso 1
5. si (p^.num > f^.num)
   entonces
      r <- creaNodo();
      r^.num <- p^.num;
      r^.liga <- lista_3(p^.liga, f);
   sino 
      r <- creaNodo();
      r^.num <- f^.num;
      r^.liga <- lista_3(p, f^.liga);
      z <- r;
6. fin de la condicional del paso 5